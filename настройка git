первая строчка для подключения git init к папке проекта
потом создать пользоааптеля
git config --local user.name "David"
git config --local user.email example@gmail.com
git status - для проверки статуса репозитория
git add -A - добавить все файлы
git add имя файла - добавить конекертный файл
git commit -a -m"first commit" - коммитит все файлы в ковычках описание контрольной точки
git log - просмотреть информацию про коммит(кто, что и когда)
git remote add origin https://github.com/DavidYaremenko/new-test.git добавляем коммит в репозиторийн на гитхабе
git push -u origin main - добавить изменения или файлы
cd .. - выйти из папки проекта
cd название папки - войти в папку
git clone (ссылка с гитхаба на репозиторий, который нужно скачать себе на комп) it (название папки куда\сосздать)
git pull - обновляет проект в репозитории, если он был изменен с другого компьютера(подтягивает запушенные изменения прямо на комп с удаленного сервера)
